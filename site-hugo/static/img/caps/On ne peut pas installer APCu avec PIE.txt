On ne peut pas installer APCu avec le nouvel outil PIE, qui remplace le packet manager PECL de php. C'est parce que il n'y a pas de binaires précompilés pour windows de dispo dessus. On va donc utiliser les repos PECL en allant chercher un DLL (spécifique à windows) et le mettre dans ext pour les extensions. 

https://windows.php.net/downloads/pecl/releases/apcu/ 

On voit bien qu'il existe des binaires windows ici. Il faut choisir la dernière version, et prendre le bon packet zip, avec la version de PHP qu'on a, pas la dernière mais (5.1.28) version du soft et (8.3) version de PHP et TS thread safe et VS16 ? en x64.

Une fois qu'on a mis le dll dans le répertoire, ne pas oublier de l'activer avec php.ini, en ajoutant la ligne extension=acpu

j'ai fait une faute de frappe, donc il faut que je le mettre en vrai (acpu) !

C:\wamp64\bin\php\php8.3.28\php.ini
http://localhost/mw-config/index.php?page=Welcome

php -m donne les modules installés

Ptn, en fait il y a deux php... celui du term qu'on a mis dans path, et celui d'apache ? Si ca se trouve, il dit qu'il y a pas acpu car c'est ailleurs ??

Apache n'utilise pas le bon php.ini, C:\wamp64\bin\apache\apache2.4.65\bin\php.ini
Il devrait utilser celui la : C:\wamp64\bin\php\php8.3.28\php.ini

APCu est installé dans PHP
mais Apache charge un autre php.ini

On doit utiliser le fichier de conf apache 
C:\wamp64\bin\apache\apache2.4.65\conf\httpd.conf